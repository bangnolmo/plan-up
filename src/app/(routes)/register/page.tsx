"use client";

import Header from "@/app/_components/Header";
import PageInfo from "@/app/_components/PageInfo";

const Register = () => {
    return (
        <>
            <Header />
            <PageInfo title="회원가입" description="플랜업 사용을 위해 재학생 인증이 필요합니다." />
        </>
    );
};

export default Register;
